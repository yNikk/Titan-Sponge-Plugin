package eu.flowtex.magicgames.manager;

public class SaveInvManager {
    //@SubscribeEvent
    //public static void onDrop(final ExtendedPlayerDropItemsEvent e) {
    //    final EntityPlayer p = e.player;
    //    final UUID id = p.getPersistentID();
    //    System.out.println(p.getPersistentID());
    //    final User u = Data.getPlayerFromUUid(id);
    //    final TeamManager tm = TeamManager.getInstance();
    //    if (tm.getPlayersTeam(u) != null && tm.getPlayersTeam(u).getLives() >= 1.0f) {
    //        e.shouldDrop = false;
    //    }
    //}
}
